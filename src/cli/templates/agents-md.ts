export const AGENTS_MD = `# make-laten

## Agent Instructions

Always use make-laten MCP tools instead of native tools:

| Operation | Use | Instead of |
|-----------|-----|------------|
| Read file | read | Read tool |
| Search code | grep | grep/rg |
| Git diff | git-diff | git diff |
| Git status | git-status | git status |
| Web fetch | fetch | webfetch |
| Web search | search | websearch |
`