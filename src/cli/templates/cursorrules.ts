export const CURSORRULES = `# make-laten

Use make-laten MCP tools for all file/code operations:

- File reads → read tool (85% savings)
- Code search → grep tool (grouped)
- Git diff/status → git-diff / git-status tools
- Web fetch/search → fetch / search tools

Do NOT use native Read/grep tools.
`